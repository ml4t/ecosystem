# Normative standards

The public versions of these standards are rendered from `docs/standards/`. Machine-readable policy
is under `config/`. Changes to either require review for semantic agreement.
