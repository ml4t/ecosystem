[project]
name = "ml4t-ecosystem"
version = "0.1.0"
description = "Shared standards and qualification tooling for ML4T libraries"
readme = "README.md"
requires-python = ">=3.12"
license = "MIT"
authors = [{ name = "Stefan Jansen", email = "stefan@applied-ai.com" }]
maintainers = [{ name = "Stefan Jansen", email = "pm@ml4trading.io" }]
keywords = [
    "finance",
    "quantitative-finance",
    "algorithmic-trading",
    "release-qualification",
    "software-standards",
]
dependencies = [
    "httpx>=0.28,<1",
    "packaging>=24,<27",
    "pyyaml>=6.0,<7",
]

[project.urls]
Homepage = "https://www.ml4trading.io/"
Documentation = "https://ml4t.github.io/ecosystem/"
Repository = "https://github.com/ml4t/ecosystem"
Issues = "https://github.com/ml4t/ecosystem/issues"
Changelog = "https://github.com/ml4t/ecosystem/releases"

[project.scripts]
ml4t-ecosystem = "ml4t_ecosystem.cli:main"

[dependency-groups]
dev = [
    "mkdocs>=1.6,<2",
    "mkdocs-material>=9.6,<10",
    "pre-commit>=4.3,<5",
    "pytest>=8.4,<10",
    "pytest-cov>=6.2,<8",
    "ruff>=0.15,<0.16",
    "ty>=0.0.7,<0.1",
]

[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ml4t_ecosystem"]

[tool.hatch.build.targets.sdist]
include = [
    "/LICENSE",
    "/README.md",
    "/pyproject.toml",
    "/src/ml4t_ecosystem",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]

[tool.pytest.ini_options]
addopts = "-ra --strict-config --strict-markers"
testpaths = ["tests"]

[tool.coverage.run]
branch = true
source = ["ml4t_ecosystem"]

[tool.coverage.report]
fail_under = 90
show_missing = true

[tool.ty.src]
include = ["src", "tests"]
