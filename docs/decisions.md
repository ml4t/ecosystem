# Decisions

Cross-library decisions are tracked under `decisions/` in the repository. A decision record is
required when a change affects multiple libraries, changes a shared release criterion, or grants a
time-limited exception.

Library-specific implementation choices remain in the owning repository.
