# Qualification status

`current.json` and `current.md` are generated locally and ignored. Validated dated snapshots are
tracked under `snapshots/` and identify the source evidence and evaluated commits.
